export enum CommonRoute {
  SIGN_IN = "sign in",
  SIGN_UP = "sign up",
}

export enum ChlidRoute {
  INDEX = "index",
  ID = "id",
}

export enum AdminRoute {
  MEASURE = "measure",
}

export enum GuardianRoute {
  PAIR = "pair",
}
